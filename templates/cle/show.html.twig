{% extends 'base.html.twig' %}

{% block title %}Cle{% endblock %}

{% block body %}
    <h1>Cle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cle.id }}</td>
            </tr>
            <tr>
                <th>Cle</th>
                <td>{{ cle.cle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cle_index') }}">back to list</a>

    <a href="{{ path('cle_edit', {'id': cle.id}) }}">edit</a>

    {{ include('cle/_delete_form.html.twig') }}
{% endblock %}
